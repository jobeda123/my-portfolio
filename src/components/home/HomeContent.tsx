import React from 'react'
import { BASIC_INFO } from '@/constants/user/basicInfo'
import { ABOUT_INFO } from '@/constants/user/aboutInfo'

function HomeContent() {
  // Get core skills for display
  const coreSkills = [
    'React & Next.js',
    'Node.js',
    'PHP/Laravel', 
    'TypeScript',
    'MySQL/PostgreSQL',
    'Docker'
  ];

  return (
      <section className="bg-gradient-to-br from-slate-50 to-gray-100 text-gray-900 lg:h-auto h-screen lg:overflow-visible overflow-y-auto lg:pt-0 pt-64">
        {/* Hero Section */}
      
      </section>
  )
}

export default HomeContent